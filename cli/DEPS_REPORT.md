**Dependency Report**

1. **click** (≥8.1.0) – Command‑line interface toolkit for building user-friendly CLI commands.
2. **httpx** (≥0.27.0) – Async HTTP client used for communicating with the Chinese Worker backend.
3. **rich** (≥13.0.0) – Rich text and beautiful formatting for terminal output.
4. **python‑dotenv** (≥1.0.0) – Loads environment variables from a `.env` file, enabling API URL configuration.
5. **prompt_toolkit** (≥3.0.0) – Provides advanced interactive input features such as auto‑completion and history during chat sessions.